version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DB_URL=mongodb://db:27017/tasks
    depends_on:
      - db

  db:
    image: mongo
    volumes:
      - db-data:/data/db

volumes:
  db-data:
